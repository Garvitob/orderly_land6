/**
 * 3% fractal-noise texture, fixed over the whole page (§5.2 "real texture").
 * Server component: no state, no effects, nothing to hydrate.
 */
export function Grain() {
  return <div className="grain" aria-hidden="true" />;
}
